import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import BoxImage from './images/BoxImage.png'
import Box from '../components/Box';

<Meta title="Consignas|Box" component={Box} />

# Box

Box es una caja que tiene un color de fondo `blue` y un tamaño de `200px x 200px`.

Su tipografía es `Arial, Helvetica, sans-serif` de color blanco y en tamaño `32px`.

Su contenido está centrado tanto horizontal como verticalmente:

<img src={BoxImage} />